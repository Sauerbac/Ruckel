{
  "comment": "ADR-0028 matrix cells with no FFmpeg encoder — no fixture can be generated by scripts/generate-fixtures.mjs. The manifest↔corpus drift test (issue 05) treats these decoders as covered. If a cell ever needs real coverage, pin an external sample by SHA-256 and remove its entry here.",
  "gaps": [
    {
      "decoder": "vc1",
      "kind": "video",
      "reason": "FFmpeg decodes VC-1/WMV3/WMV9 but has never shipped a VC-1 encoder."
    },
    {
      "decoder": "wmapro",
      "kind": "audio",
      "reason": "FFmpeg decodes WMA Pro but has no wmapro encoder (only wmav1/wmav2)."
    }
  ]
}
